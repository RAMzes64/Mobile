<Window x:Class="WpfCounter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCounter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="300">

    <Window.InputBindings>
        <KeyBinding Key="F2" Command="{Binding IncrementCommand}"/>
        <KeyBinding Key="F3" Command="{Binding DecrementCommand}"/>
        <KeyBinding Key="F4" Command="{Binding ResetCommand}"/>
    </Window.InputBindings>
    
    <StackPanel>
        <TextBlock 
            Text="{Binding Counter}" 
            FontSize="60"
            HorizontalAlignment="Center"
            />

        <Button Content="+" Command="{Binding IncrementCommand}"/>
        <Button Content="-" Command="{Binding DecrementCommand}"/>
        <Button Content="Reset" Command="{Binding ResetCommand}"/>

        <Button Content="+10"
                Command="{Binding AddCommand}"
                CommandParameter="10"/>
    </StackPanel>
</Window>
