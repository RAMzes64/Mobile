<Window x:Class="WpfTodoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTodoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="300">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top"
                   Text="Список задач"
                   FontSize="24"
                   FontWeight="Bold"/>
        <Line></Line>

        <TextBox DockPanel.Dock="Top"
                 Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"/>
        <Button DockPanel.Dock="Top" Content="Добавить"/>

        <Button DockPanel.Dock="Top" Content="Удалить"/>
        <Button DockPanel.Dock="Top" Content="Удалить выполненные"/>
        <ListBox ItemsSource="{Binding Todos}"
                 SelectedItem="{Binding SelectedTodo}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    
                <DockPanel>
                    <CheckBox IsChecked="{Binding IsDone}"/>
                    <TextBox Text="{Binding Title}"/>
                </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
