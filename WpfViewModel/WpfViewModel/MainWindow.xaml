<Window x:Class="WpfViewModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfViewModel"
        mc:Ignorable="d"
        Title="WpfViewModel" Height="450" Width="300">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="10">
        <TextBlock Text="Имя:"/>
        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Фамилия:"/>
        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Возраст:"/>
        <TextBox Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock FontSize="20" Background="LightBlue">
            <Run Text="{Binding FirstName}"/>
            <Run Text="{Binding LastName}"/>
            <Run Text="{Binding Age}"/>
        </TextBlock>
        <TextBlock Background="Bisque"  Text="{Binding FullName}"/>
    </StackPanel>
</Window>
